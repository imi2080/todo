# 개인용 TODO 웹 서비스 PRD

- 문서 버전: v1.0
- 작성일: 2026-09-25
- 상태: 초안 (M0 착수 전)

---

## 1. 제품 개요

**한 줄 정의**: 나 혼자 쓰는, 빠르고 단순한 TODO 관리 웹 서비스.

**프로젝트 성격 (이중 목표)**
- **제품 목표**: 매일 쓸 수 있는 개인용 TODO 웹 서비스
- **학습 목표**: Claude Code로 풀스택 개발을 하면서 프론트엔드, 백엔드, API가 하나의 프로젝트 안에서 어떻게 맞물리는지 직접 경험한다.

> 학습이 목적이므로 기능을 늘리기보다 각 계층의 역할과 경계를 이해하는 데 우선순위를 둔다.

**배경 및 문제**
- 기존 TODO 앱은 협업, 알림, 유료 플랜 등 개인에게 불필요한 기능이 많고 무겁다.
- 내 데이터를 직접 소유하고, 필요한 기능만 갖춘 가벼운 도구가 필요하다.

**제품 목표**
- 할 일을 3초 안에 추가하고, 오늘 할 일을 한눈에 확인한다.
- 직접 호스팅할 수 있고 유지보수 부담이 작다.

**비목표 (이번 범위 제외)**
- 다중 사용자, 팀 협업, 공유
- 푸시 알림, 모바일 네이티브 앱
- 캘린더 연동, AI 추천
- React, Vue 같은 프레임워크, 빌드 도구(Webpack, Vite), ORM 고급 기능 (계층 구조를 가리지 않도록 의도적으로 제외)

**대상 사용자**: 본인 1명(단일 사용자). 데스크톱과 모바일 브라우저에서 사용한다.

---

## 2. 학습 목표

| # | 학습 주제 | 이 프로젝트에서 경험하는 방식 | 확인 방법 |
|---|-----------|------------------------------|-----------|
| L1 | 프론트엔드-백엔드-API 구조 이해 | 브라우저(HTML/CSS/JS) → HTTP 요청 → FastAPI 라우터 → DB의 전체 흐름을 직접 구현한다. | 기능 하나(예: 할 일 추가)의 요청 흐름을 다이어그램으로 직접 그릴 수 있다. |
| L2 | REST API 설계 | 리소스(`todos`, `categories`), HTTP 메서드, 상태 코드, 에러 응답 형식을 정한다. | `/docs`(Swagger)에서 모든 엔드포인트를 호출해 볼 수 있다. |
| L3 | 프론트-백엔드 계약(스키마) | Pydantic 스키마가 곧 API 계약이며, 프론트의 `fetch` 코드가 이를 따른다. | 스키마를 바꾸면 어느 코드가 영향받는지 설명할 수 있다. |
| L4 | 상태와 영속성 | 서버(DB)가 진실의 원천이고, 프론트는 화면 상태만 관리한다. | 새로고침 후에도 화면이 서버 데이터와 일치한다. |
| L5 | 인증·보안 기초 | 쿠키 세션, 비밀번호 해시, XSS 방어를 구현한다. | 비로그인 상태에서 API가 401을 반환한다. |
| L6 | Claude Code 활용 워크플로 | 5절의 방식으로 개발한다. | 마일스톤마다 회고를 남긴다. |

---

## 3. 핵심 기능 (5개)

| # | 기능 | 설명 | 완료 조건 (Acceptance Criteria) |
|---|------|------|------|
| F1 | 할 일 CRUD + 완료 처리 | 제목·메모를 입력해 생성, 수정, 삭제하고 체크박스로 완료를 토글한다. | 입력창에서 Enter로 즉시 추가된다. 완료 시 취소선이 표시되고 새로고침 후에도 유지된다. |
| F2 | 마감일·우선순위 | 마감일과 우선순위(높음/보통/낮음)를 지정한다. | 마감일이 지난 항목은 시각적으로 강조된다. 우선순위별 색상이 구분된다. |
| F3 | 카테고리(태그) 분류 | 항목에 카테고리를 1개 지정하고 카테고리를 관리(추가·이름 변경·삭제)한다. | 카테고리 삭제 시 소속 항목은 "미분류"가 된다. |
| F4 | 검색·필터·정렬 | 제목 키워드 검색, 상태·카테고리·우선순위 필터, 마감일·생성일·우선순위 정렬을 지원한다. | 조건 변경 시 페이지 이동 없이 목록이 갱신된다. |
| F5 | 스마트 뷰 | "오늘 / 예정 / 지연 / 완료" 뷰를 제공한다. | 각 뷰의 건수가 사이드바에 표시된다. 기본 진입 화면은 "오늘"이다. |

---

## 4. 기술 스택 및 아키텍처

| 영역 | 선택 | 비고 |
|------|------|------|
| 백엔드 | **FastAPI** (Python 3.11+) | REST API, Pydantic 검증, OpenAPI 문서 자동 생성 |
| DB | SQLite + SQLAlchemy | 단일 사용자에 적합, 파일 하나로 백업 가능 |
| 프론트엔드 | **HTML / CSS / Vanilla JavaScript** | 빌드 도구·프레임워크 없이 `fetch`로 API 호출 |
| 정적 파일 서빙 | FastAPI `StaticFiles` | 프론트와 백엔드를 한 프로세스로 배포 |
| 서버 실행 | Uvicorn | 필요 시 Docker로 패키징 |
| 인증 | 단일 계정 (비밀번호는 환경변수로 설정, 해시 검증) + HttpOnly 쿠키 세션 | 외부 노출 시 필수 |

### 4.1 아키텍처 학습 지도

```
[브라우저]                         [FastAPI 서버]                  [저장소]
 index.html  ── 구조                 routers/  ── HTTP 처리
 style.css   ── 표현                 schemas.py ── 요청/응답 검증   ──►  SQLite
 app.js      ── 동작/상태 ─ fetch ─► models.py  ── DB 매핑
             ◄── JSON 응답 ───────  (auth 미들웨어)
```

**계층별 책임 원칙**
- **프론트엔드**: 화면 렌더링, 사용자 입력 처리, API 호출만 담당한다. 비즈니스 규칙은 두지 않는다.
- **API 계층**: 요청 검증, 인증, 상태 코드 결정을 담당한다.
- **데이터 계층**: 저장과 조회만 담당하며 HTTP를 모른다.

**학습을 위한 코드 구성 지침**
- 프론트 스크립트는 `api.js`(통신), `ui.js`(렌더링), `app.js`(연결)로 분리해 계층 개념을 코드에서도 보이게 한다.
- 각 계층 파일 상단에 그 파일의 책임을 한 줄 주석으로 적는다.

### 4.2 프로젝트 구조(안)

```
todo/
├─ app/
│  ├─ main.py        # FastAPI 앱, 라우터 등록, 정적 파일 마운트
│  ├─ models.py      # SQLAlchemy 모델
│  ├─ schemas.py     # Pydantic 스키마
│  ├─ routers/       # todos.py, categories.py, auth.py
│  └─ db.py
├─ static/           # index.html, style.css, api.js, ui.js, app.js
├─ tests/
├─ requirements.txt
├─ PRD.md
└─ CLAUDE.md
```

### 4.3 데이터 모델

- `Todo`: id, title, note, is_done, priority(0~2), due_date, category_id(nullable), created_at, updated_at
- `Category`: id, name (unique)

### 4.4 주요 API

- `GET/POST /api/todos`, `PATCH/DELETE /api/todos/{id}`
  - `GET`의 쿼리: `q`, `status`, `category_id`, `priority`, `sort`, `view`
- `GET/POST /api/categories`, `PATCH/DELETE /api/categories/{id}`
- `POST /api/auth/login`, `POST /api/auth/logout`
- `GET /api/health` (M0 확인용)

---

## 5. Claude Code 활용 방식

**개발 원칙**
1. **먼저 이해하고, 나중에 생성한다.** 코드 작성을 요청하기 전에 "이 기능은 프론트, API, DB에서 각각 어떤 변경이 필요한가?"를 먼저 묻는다.
2. **작은 단위로 요청한다.** 한 번에 하나의 계층, 하나의 기능만 요청한다. (예: "`POST /api/todos` 엔드포인트와 스키마만 만들어줘")
3. **생성된 코드를 읽고 설명을 요청한다.** 이해되지 않는 부분은 "이 줄이 왜 필요한지 설명해줘"라고 묻는다.
4. **직접 실행하고 확인한다.** 코드가 나오면 서버를 띄워 `/docs`나 브라우저 개발자 도구(Network 탭)로 요청/응답을 직접 관찰한다.
5. **직접 고쳐 본다.** 기능마다 최소 한 번은 스스로 수정해 본 뒤 Claude Code에 리뷰를 요청한다.

**활용 기능**

| Claude Code 기능 | 프로젝트에서의 용도 |
|------------------|---------------------|
| `CLAUDE.md` | 프로젝트 구조, 계층 책임 원칙, 코딩 규칙, 실행 방법을 기록해 세션마다 같은 맥락을 유지한다. |
| Plan 모드 | 기능 구현 전에 계층별 변경 계획을 먼저 검토한다. |
| 코드 리뷰(`/code-review`) | 마일스톤 종료 시 버그와 개선점을 점검한다. |
| 보안 리뷰(`/security-review`) | M4 인증 구현 후 취약점을 점검한다. |
| 테스트 생성·실행 | pytest로 API 동작을 검증하고, 실패 원인을 함께 분석한다. |

**프롬프트 예시**
- 설계: "할 일 완료 토글 기능을 추가하려고 해. 프론트, API, DB 각각 어떤 변경이 필요한지 계획만 먼저 알려줘."
- 구현: "계획대로 `PATCH /api/todos/{id}` 엔드포인트와 Pydantic 스키마만 구현해줘."
- 학습: "방금 만든 코드에서 요청이 들어와 DB에 저장되기까지의 흐름을 단계별로 설명해줘."
- 디버깅: "프론트에서 422 에러가 나. 요청 본문과 스키마를 비교해서 원인을 찾아줘."

---

## 6. 비기능 요구사항

> 개인용·학습용 프로젝트이므로 꼭 필요한 항목만 둔다. 수치 목표, 자동 백업, 다크 모드, 다중 브라우저 검증 등은 범위에서 제외한다.

**성능**
- 할 일이 수천 건이어도 목록과 검색이 체감상 즉시 반응한다.
- 외부 라이브러리 없이 동작한다.

**보안**
- 로그인 없이는 `/api/*`에 접근할 수 없다 (로그인 제외).
- 비밀번호는 해시로 검증하고 환경변수로 설정하며, 코드에 하드코딩하지 않는다.
- 세션 쿠키는 HttpOnly로 설정한다.
- SQL 인젝션은 ORM으로, XSS는 `textContent` 렌더링으로 방지한다.

**데이터 보존**
- 모든 변경은 즉시 DB에 반영된다 (새로고침해도 유실되지 않는다).
- 백업은 SQLite 파일을 수동으로 복사하는 방식으로 한다.

**호환성**
- 최신 Chrome 또는 Edge에서 동작한다.
- 모바일 폭(약 360px)과 데스크톱 폭 모두에서 레이아웃이 깨지지 않는다.

**사용성**
- 키보드만으로 할 일 추가, 완료, 삭제가 가능하다.

**유지보수성**
- API는 OpenAPI(`/docs`)로 자동 문서화한다.
- 핵심 API(CRUD, 인증)에 pytest 테스트를 작성한다.
- 설정값은 환경변수로 분리한다.

---

## 7. 성공 지표

**제품 지표**
- 주 5일 이상 접속한다.
- 할 일 추가는 3초 이내에 끝난다.
- 배포 후 2주간 데이터 유실과 장애가 없다.

**학습 지표**
- 코드를 보지 않고 "할 일 추가" 요청의 전체 흐름(브라우저 → 라우터 → 스키마 검증 → DB → JSON 응답 → 화면 갱신)을 설명할 수 있다.
- 새 필드(예: 메모 길이 제한)를 프론트, 스키마, DB에 모두 스스로 반영할 수 있다.
- 마일스톤별 회고가 5개 모두 작성된다.
- Claude Code가 생성한 코드 중 이해하지 못한 채 넘어간 부분이 없다.

---

## 8. 마일스톤

| 단계 | 개발 내용 | 학습 포인트 | 산출물 |
|------|-----------|-------------|--------|
| **M0** | 환경 설정, `CLAUDE.md` 작성, "Hello API" (`GET /api/health`) + 정적 페이지에서 호출 | 프론트가 백엔드를 호출하는 가장 작은 흐름 | 동작하는 최소 풀스택 |
| **M1** | DB 모델, 할 일 CRUD API, 기본 화면 (F1) | REST 설계, 스키마, `fetch`, DOM 렌더링 | 요청 흐름 다이어그램 |
| **M2** | 마감일·우선순위(F2), 카테고리(F3) | 테이블 관계, 스키마 변경 시 전 계층 영향 | 변경 영향 분석 메모 |
| **M3** | 검색·필터·정렬(F4), 스마트 뷰(F5) | 쿼리 파라미터 설계, 서버 필터링 vs 클라이언트 필터링 | 두 방식의 비교 노트 |
| **M4** | 인증, 반응형, 테스트, 배포 | 쿠키 세션, 보안, 테스트, 배포 | 배포된 서비스 |

각 마일스톤이 끝나면 **회고 3줄**을 남긴다.
- 새로 이해한 것
- Claude Code가 잘 도와준 점과 아쉬운 점
- 다음에 다르게 할 것

---

## 9. 리스크

| 리스크 | 대응 |
|--------|------|
| 코드를 그대로 받아들이기만 해 학습 효과가 떨어진다. | 5절 원칙 3~5를 지키고, 기능마다 직접 수정하는 단계를 둔다. |
| 기능 욕심으로 범위가 커져 학습 초점이 흐려진다. | 핵심 기능 5개를 고정하고 추가 아이디어는 백로그로 분리한다. |
| 바닐라 JS 코드가 커지면 구조가 무너진다. | 파일 분리 원칙(`api.js`/`ui.js`/`app.js`)을 유지하고, 필요하면 M4 이후 프레임워크 도입을 별도 학습으로 검토한다. |

---

## 10. 오픈 이슈

- 카테고리를 단일 지정으로 할지, 다대다 태그로 확장할지 (현재안: 단일)
- 배포 대상 (로컬 PC, 클라우드 VM, PaaS 등) 미정
- 인증 방식 (환경변수 단일 계정 vs 최초 실행 시 계정 생성) 미정
